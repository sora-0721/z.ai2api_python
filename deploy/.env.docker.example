# Docker 环境变量配置示例
# 将此文件复制为 .env.docker 并修改配置

# ==================== 认证配置 ====================
# 管理后台登录密码（⚠️ 强烈建议修改为强密码）
ADMIN_PASSWORD=admin123

# 客户端 API 访问密钥（必填）
AUTH_TOKEN=sk-your-api-key

# 跳过 API 认证（仅开发环境使用，生产环境设为 false）
SKIP_AUTH_TOKEN=false

# ==================== 服务配置 ====================
# 服务监听端口
LISTEN_PORT=8080

# 服务名称
SERVICE_NAME=z-ai2api-server

# ==================== 功能开关 ====================
# 调试日志（生产环境建议设为 false）
DEBUG_LOGGING=true

# Z.AI 匿名模式
ANONYMOUS_MODE=true

# Function Call 功能
TOOL_SUPPORT=true

# 工具调用扫描限制（字符数）
SCAN_LIMIT=200000

# ==================== Token 池配置 ====================
# Token 失败阈值
TOKEN_FAILURE_THRESHOLD=3

# Token 恢复超时（秒）
TOKEN_RECOVERY_TIMEOUT=1800

# ==================== LongCat 配置 ====================
# LongCat Token（可选）
# LONGCAT_TOKEN=your_passport_token_here

# ==================== 提供商配置 ====================
# 默认提供商：zai/k2think/longcat
DEFAULT_PROVIDER=zai

# ==================== 安全配置 ====================
# Session 密钥（建议生成随机字符串）
SESSION_SECRET_KEY=your-secret-key-change-in-production

# ==================== 注意事项 ====================
# 1. 生产环境请务必修改 ADMIN_PASSWORD 为强密码
# 2. AUTH_TOKEN 用于客户端访问 API，需保密
# 3. 不要将此文件提交到 Git 仓库
# 4. 推荐使用 Docker Secrets 或环境变量管理工具
